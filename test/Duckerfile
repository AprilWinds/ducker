# 测试 ducker 功能的示例 Duckerfile
FROM alpine:latest

# 设置工作目录
WORKDIR /app

# 设置环境变量
ENV APP_NAME=ducker-test APP_VERSION=1.0

# 复制应用文件
COPY app.sh /app/app.sh

# 暴露端口
EXPOSE 8080

# 启动命令
CMD ["/bin/sh", "/app/app.sh"]
